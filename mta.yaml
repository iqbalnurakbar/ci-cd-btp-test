_schema-version: "3.2"
ID: dev
description: Dev
version: 0.0.1
modules:
- name: dev-router
  type: approuter.nodejs
  path: router
  requires:
  - name: dev-html5-repo-runtime
  - name: dev-uaa
  - name: dev-destination-service
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  parameters:
    disk-quota: 256M
    memory: 256M
- name: dev-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: dev-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - btpdeploy.zip
      name: btpdeploy
      target-path: resources/
- name: btpdeploy
  type: html5
  path: btpdeploy
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: dev-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: dev-${space-guid}
    service: xsuaa
    service-plan: application
- name: dev-html5-repo-runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: dev-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: dev-destination-service
    service-plan: lite
- name: dev-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: dev-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
